#include "iostream"

using namespace std;

int main() {
    unsigned short year; // Current year always +ve, no need for anything larger than short
    unsigned char hours; // 0-255 unisgned, no need for negatives for elapsed hours in day 
    float temperature; // Decimal point needed, can be -ve so signed however no such data type as unsigned float 
}